{
    "runOnSave.statusMessageTimeout": 3000,
    "runOnSave.commands": [
        {
            // Match less files except names start with `_`.
            "globMatch": "**/pandoc-revealjs.md",
            "command": "${fileDirname}/run-pandoc.sh",
            "runIn": "terminal"
        }
    ]
}